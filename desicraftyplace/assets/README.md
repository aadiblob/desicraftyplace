# Image uploads

Place the final logo or profile photo in this folder.

Recommended profile image:

- JPG, PNG, or WebP
- Square or portrait orientation
- At least 800 × 800 pixels
- A clear subject near the center, because the image is cropped into an organic circle

After uploading, add the path to `src/content.js`, such as `./assets/mom-profile.jpg`.
